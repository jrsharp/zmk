# nRF52 specific: Use polling mode for wired split
# Async and interrupt modes have issues on nRF52 UARTE
CONFIG_ZMK_SPLIT_WIRED_UART_MODE_ASYNC=n
CONFIG_ZMK_SPLIT_WIRED_UART_MODE_INTERRUPT=n
CONFIG_ZMK_SPLIT_WIRED_UART_MODE_POLLING=y
